version: '3.8'
services:
  rr-discord-app:
    build: ./app/
    volumes:
      - ./app/:/usr/src/app/
    container_name: rr-discord-app
    env_file:
      - ./app/.env.compose
    networks:
      frontend:
  rr-postgres:
    image: postgres:13-alpine
    container_name: rr-postgres
    ports:
    - "5432:5432"
    volumes:
      - rr_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=rr_admin
      - POSTGRES_PASSWORD=rr_pass
      - POSTGRES_DB=rr_data
    networks:
      backend:
  rr-api:
    build: ./api/
    container_name: rr-api
    volumes:
      - ./api/:/usr/src/api/
    ports:
    - "5000:5000"
    env_file:
      - ./api/app/.env.compose
    networks:
      frontend:
      backend:
volumes:
  rr_data:
networks:
  frontend:
  backend: